# Test Plan

Status: proposed, pre-implementation. Strategy and per-phase plan; concrete test files land alongside the code they cover during each phase in [progress.md](../docs/progress.md).

## 1. Principles

- **The pricing engine and the inventory ledger get the heaviest test investment.** They are money and stock — bugs there are the most expensive kind. Everything else follows normal test discipline; these two get exhaustive treatment.
- **Unit tests for pure logic** (`packages/pricing-engine`, `packages/validation`) — no DB, no HTTP, fast, run on every commit.
- **Integration tests for `apps/api` modules** against a real MongoDB replica set (in-memory or containerized) — because transaction semantics and ledger invariants can't be trusted against a mocked driver.
- **E2E tests for cross-channel flows** (browse → cart → checkout → invoice; B2B PO → order → credit approval → invoice → payment allocation) using Playwright against a running stack.
- **No test suite blocks on infrastructure that isn't set up yet** — each phase's tests run against that phase's actual dependencies, added incrementally rather than a big-bang test harness upfront.

## 2. Pricing engine (packages/pricing-engine) — **implemented, see §14**

- Unit tests, table-driven: given a fixed set of (metal rate, purity, weight, making-charge rule, wastage rule, stone value, discount, tax rule) inputs, assert exact output to the paise/rupee.
- Snapshot tests against a set of known-good hand-calculated examples (finance/ops sign-off on the expected numbers before these are locked in as regression baselines).
- Rule-priority/conflict tests: overlapping `PricingRule`s at different scopes (global vs. category vs. customer) resolve in the documented priority order.
- Property-based test: total = metalValue + makingCharge + wastage + stoneValue − discount + tax, always, for randomized valid inputs (catches arithmetic regressions cheaply).
- Explicit regression test: changing today's `MetalRate` must not change a previously-created `PriceSnapshot`'s computed values when re-read.

## 3. Inventory ledger (apps/api inventory module)

- Every `movementType` has a test asserting the correct `fromStatus`→`toStatus` transition and correct ledger entry shape.
- Illegal transition attempts (e.g. `SOLD` → `AVAILABLE` without a `RETURN` transaction) are rejected.
- Concurrency test: two simultaneous reservation attempts on the same unit `InventoryItem` — exactly one succeeds, the other gets a typed "unavailable" error, no double-reservation under load (run with real concurrent requests, not sequential mocked calls).
- Reconciliation test: replaying all `inventoryLedger` entries for an item reproduces its cached `status`/`location` exactly — run as an actual scheduled job in later phases, tested here as a pure function first.
- Batch (fungible) item weight-delta arithmetic: sequential partial issues/receipts never drift the running total (rounding).

## 4. Job work reconciliation (Phase 6)

- Balanced case: issued = returned + finished + wastage + loss exactly → `reconciliationStatus: BALANCED`, order can close.
- Discrepancy case: numbers don't balance within tolerance → `DISCREPANCY_PENDING_REVIEW`, order cannot close without manual override, override is itself audit-logged.
- Partial return over multiple shipments from the job worker accumulates correctly before final reconciliation.

## 5. B2B credit (Phase 4)

- Order within credit limit confirms without approval step.
- Order exceeding available credit is blocked and routed to approval workflow.
- Outstanding/overdue figures recompute correctly as invoices are issued and payments allocated (including partial payment across multiple invoices).

## 6. Compliance (Phase 7)

- Intra-state transaction resolves to CGST+SGST; inter-state resolves to IGST — table-driven across buyer/seller state combinations.
- `TaxRule` effective-dating: a rate change on a given date does not affect invoices dated before it, and does affect ones on/after it.
- HUID enforcement: an item above the configured hallmarking-mandatory threshold cannot transition to B2C-`AVAILABLE` without a recorded HUID.

## 7. API layer (all phases)

- Every module's service functions have integration tests against a test MongoDB instance (replica set, for transaction support).
- Authorization tests per route: each of the three channel identities (ERP staff role, B2B buyer, B2C customer) gets exactly the access the role should have — explicit "should be forbidden" tests, not just "should succeed" tests.
- Input validation: `packages/validation` Zod schemas rejected/accepted cases mirrored between a unit test on the schema and an integration test that the API actually enforces it.

## 8. Frontend (all phases)

- Component tests for `packages/ui` primitives (render, states: default/loading/error/empty, keyboard interaction on Combobox/Command palette/Dialog).
- No business logic to unit test in components by design (business-rules.md §7.1) — frontend tests focus on rendering/interaction correctness, not recomputing prices or credit checks client-side.
- Playwright E2E per phase's critical path (see §9), run against a seeded test database, never against production data.

## 9. E2E critical paths (added as each phase ships)

- Phase 3: guest browses catalogue → adds to cart → checkout → payment succeeds → order confirmed → inventory reserved then sold → invoice generated.
- Phase 3: checkout payment fails/expires → inventory reservation released → item available again.
- Phase 4: B2B buyer submits PO-based order → exceeds credit limit → approval requested → approved → order confirmed → offline payment recorded → invoice partially paid → outstanding reflects remainder.
- Phase 6: job work order issued → partial return → finished goods received → reconciliation balances → order closed.

## 10. What's explicitly out of scope for automated testing (for now)

- Visual regression testing of the design system — deferred until the component library is stable; revisit in Phase 8.
- Load/performance testing beyond the reservation-concurrency test in §3 — a dedicated load test pass is called out as part of Phase 8 (architecture.md risk #2/#3), not before there's a real system to load-test.

## 11. Authentication & authorization (implemented — `apps/api`, 129 tests + 27 browser checks)

Run: `pnpm --filter @jewellery/api test` (Vitest, in-memory MongoDB replica set; bcrypt cost 4 in tests only).

- **Policy integrity (`role-matrix.test.ts`)** — all 13 roles defined; every granted permission exists; `SUPER_ADMIN` = everything; `ADMIN` = everything minus `settings.manage_roles`; only `SUPER_ADMIN` can manage roles; `settings.*` only for the two admin roles; `VIEWER` strictly `*.view`; separation of duties for `inventory.approve_adjustment`, `b2b.override_credit`, `accounting.create_payment`, `sales.override_price`.
- **Tokens (`tokens.test.ts`)** — round-trip; identity-only claims; `TOKEN_EXPIRED` vs `UNAUTHENTICATED`; wrong secret/audience/issuer; tampered payload; `alg: none`; missing `sid`; opaque-token entropy/parsing/constant-time compare.
- **Authentication service (`auth.service.test.ts`)** — login success; identical failure for unknown user / wrong password / inactive / locked; lockout and recovery; audit reasons without the password; refresh rotation, **reuse detection revoking the session**, concurrent-refresh race (exactly one wins), idle and absolute expiry, deactivated user; per-request revocation on logout / deactivation / role change / inactive role; logout idempotence and logout-all isolation; change-password; full password-reset matrix (unknown email sends nothing, hashed storage, single-use, expired, superseded link, forged secret doesn't burn the real token, inactive account, policy).
- **HTTP (`auth.routes.test.ts`)** — cookie flags (HttpOnly, SameSite=Strict, Path, Secure), refresh token never in body/header/query, CSRF origin check, cookie rotation and clearing, rate limiting (login, forgot-password), NoSQL-injection-shaped bodies → 400, security headers, CORS allow-list.
- **Authorization (`authorization.test.ts`)** — every protected endpoint → 401 with no/expired/tampered/revoked token; **13 roles × every endpoint** allowed exactly where the matrix says and 403 elsewhere; no-role user; client-claimed role headers ignored; live permission/deactivation changes; no privilege escalation (ADMIN can't mint/modify SUPER_ADMIN, no self role-change/deactivation, PATCH can't smuggle roles/email/hash); custom roles; audit content, secret-free audit dump, audit-log endpoint guarded and itself audited, audit immutability.
- **Browser E2E (Playwright against the real API + ERP, 27 checks)** — deep-link → login → back, generic login error, client validation, open-redirect protection, per-role sidebars, forbidden URL, session restore on reload, no token in web storage, cookie flags, real sign-out, guarded again after logout, forgot/reset pages, and a `VIEWER` calling `/api/users` + `/api/audit-logs` directly with its own token → 403. The script is ad hoc (scratch), not yet a committed suite — promote it to `apps/erp/e2e` when the ERP gets more screens.
- **Gaps** — no test yet for concurrent multi-tab refresh in a real browser, load/rate-limit behaviour across multiple API instances (needs the Redis store), or a real email transport.

## 12. Product Master / catalogue (implemented — `apps/api`, 77 tests + 63 browser checks)

Run: `pnpm --filter @jewellery/api test` (schema/unit tests need no DB; the HTTP suite uses the in-memory replica set).

- **Schemas (`catalog.validation.test.ts`)** — `slugify` (diacritics, `&`, truncation), SKU uppercasing/defaults (channels off, active), tag normalisation/de-dupe/limits, https-only videos, image-key shape (no URLs/traversal/foreign extensions), net ≤ gross, SKU stripped from updates, `null`-clears vs. no default leakage into partial updates, list-query coercion and rejection (page size cap, unknown sort, non-boolean flags), bulk union + 200-id cap.
- **Media (`image-validation.test.ts`)** — PNG/JPEG/WebP by bytes; SVG, HTML, GIF, a WAV `RIFF`, empty and truncated files refused; disk storage refuses `..`/absolute/foreign-extension keys.
- **HTTP (`catalog.routes.test.ts`)** — **13 roles × every catalogue endpoint** (reads need `catalog.view`, writes/upload need `catalog.manage`, nothing else decides); stock visible only with `inventory.view` (custom catalogue-only role); create/derive-slug/uppercase SKU; duplicate SKU (case-insensitive, across the variant namespace); dangling metal/category/collection/image and purity-for-metal rejected; SKU immutable, `null` clears, purity re-validated on metal change; search (multi-word AND, variant SKU, regex metacharacters literal), every filter and their AND-combination, case-insensitive sort, paging/out-of-range, bad params → 400; bulk (idempotent `modified`, set/clear category, add/remove collection, empty/oversized/unknown/dangling); variants (CRUD, product-scoping, SKU namespace, delete blocked by inventory); product delete (guard by inventory; product edits never create/alter `InventoryItem`s); categories (tree/counts, cycles, delete guards); collections (detach-on-delete); upload/serving (content-sniffed type, renamed HTML/SVG refused, hostile filenames ignored, 413, 403 for read-only, traversal → 404, public read with nosniff/CORP/immutable); audit content and denied writes.
- **Seed (`seed/catalog.seed.test.ts`)** — generated PNGs are accepted by the validator; every seeded product has a purity valid for its metal, existing category/collections, uploaded images and net ≤ gross.
- **Browser (Playwright, scratch script)** — list/search/filter/sort/paging state in the URL and surviving reload; variant-SKU search; thumbnails loading cross-origin; bulk selection → confirm → result, selection cleared on filter change; create with validation, image upload (and a fake `.png` rejected server-side), duplicate-SKU inline error; detail, variants, edit (SKU locked), deactivate, delete; category tree/sub-category/refused delete/parent picker; collections CRUD and filtered-list link; **read-only role**: no create/bulk controls, forbidden on `/new`, 403 calling the bulk API directly; **390 px**: cards instead of table, filter drawer, no horizontal overflow on list/detail/form.
- **Gaps** — no S3 adapter test (adapter not written), no concurrent-edit conflict test (last write wins), no load test of the list against a large catalogue, no automated visual regression.

## 13. Jewellery inventory (implemented — `apps/api`, 172 tests + 100 browser checks)

Run: `pnpm --filter @jewellery/api test`. The domain suites use the in-memory MongoDB **replica set** with real transactions and real parallel operations — no mocked driver — because the guarantees under test (atomic multi-document commit, write-conflict retry) only exist there.

- **Pure logic** — `movement-rules.test.ts` (every movement type has a rule; **no rule permits a status change the physical graph forbids**; each outbound movement has exactly one way back; destination-kind bindings; only receipts/adjustments create stock; owned-stock and available-for-sale definitions), `valuation.test.ts` (metal value maths, own-purity vs derived rate, no-rate note), `reconciliation.test.ts` (`replayLedger`: order-independence, sequence gaps, status-chain breaks, balance ≠ previous + delta, legitimate re-weigh), `inventory.validation.test.ts` (invalid weights: zero/negative/NaN/∞/4 decimals/absurd/stone ≥ gross; HUID length/charset/case/implied hallmark; barcode alphabet; posting contract — duplicate items, >100, removed movement names, zero deltas; request schemas; list/summary queries; scan-code parsing incl. scanner CR/LF/Tab, hostile input, label round-trip).
- **Operations (`stock-operations.test.ts`, 75)**
  - *Receiving:* ledger entry #1, allocated codes distinct under concurrency; **duplicate HUID** (typed error naming it, no orphan rows, case-insensitive, three racing creates → one), duplicate code/barcode, HUID set-once/immutable via identifiers; **invalid weights** (each refused with nothing written); wrong purity; unit quantity > 1; partner/unknown location.
  - *Reservation / release / sale / return:* hold recorded with its order; second reservation refused; all-or-nothing across pieces; release by holder, refused for another order, forced override; sale of a held piece only by its order; **no double sale**; nothing sellable in transit/hallmarking/job work/repair; **return** (SOLD→RETURNED into a stock location only), inspection outcomes, full circle; expiry sweep (lapsed released as System, live/open-ended kept, concurrently-sold skipped, idempotent).
  - *Transfers:* dispatch → IN_TRANSIT at the destination with `TRANSFER_OUT`; receive; partial receive; cancel; cancel after partial; closed transfers can't be re-closed; **all-or-nothing** (one ineligible piece stops everything, nothing written); source/destination validation; in-transit pieces can't be reserved/sold/re-transferred; damaged-in-transit can't be received as fine.
  - *Partner movements:* hallmarking with HUID (pending → hallmarked), HUID clash rolls the whole movement back, HUID immutability, destination-kind binding for each movement, way-back lands in stock, full trips for job work/repair/manufacturing, movements refused from statuses that don't own them.
  - *Adjustments:* a request changes nothing (no ledger row); different-person approval posts the entry with the approver as actor; **self-approval refused**; re-weigh recomputes net/fine with deltas and balance; impossible requests refused at request time; **stale** request refused at approval; reject; five simultaneous approvals apply once; SCRAP/MELTING movement types and no way back from melting.
  - ***Negative stock*:** over-withdrawal of quantity or weight refused with nothing written, draw-down to exactly zero allowed then refused, all-or-nothing across lines, units refuse deltas, batches refuse reservation.
  - ***Concurrent operations* (real parallel transactions):** 8 simultaneous reservations → exactly one; sale vs reservation racing (3 rounds); two transfers of one piece to different places → one dispatch, one transfer record; receive vs receive vs cancel → once; **12 withdrawals of 10 from a 100-unit batch → exactly 10 succeed, never negative, sequences 1..11 gap-free**; 20 unrelated pieces reserved in parallel all succeed (no over-serialisation); interleaved reserve/release/sell/transfer storms leave every ledger and cache in agreement.
  - *History & integrity:* a piece's whole life reads off its ledger in order, gap-free and status-chained, with location at each step; every entry carries who/why/reference; ledger and transactions immutable; a direct edit of the cached item is **detected** by reconciliation; a duplicate `(itemId, sequence)` is impossible.
- **HTTP (`inventory.routes.test.ts`, 47)** — unauthenticated → 401 on every endpoint; **all 13 roles × all 22 endpoints** allowed exactly where the permission matrix says; sale/return not reachable over HTTP; client-claimed roles ignored; list (rich rows, search across code/barcode/HUID/SKU/name/variant with regex-safe literal matching, every filter and their AND-combination, sort, filtered totals vs page, bad params → 400); detail (product/location, valuation from the current rate, derived-purity note, no-rate note, future-dated rate ignored, 404/400, no Mongo internals); summaries by location/SKU/purity/metal (counts, weights, cost, status mix, in-transit counted at destination but not available, scope, filters); ledger (filters, enrichment, paging, System attribution, bad params); per-item audit (actor, request id, no secrets); scan (QR/code/barcode/serial/HUID, unknown → 200 with no item, hostile input); receive (201, validation with named fields, duplicate HUID 409, unknown refs, smuggled fields ignored, HUID set-once); reserve/release (ownership: only the holder or an approver; forced release needs authority; malformed bodies); transfers and partner movements end to end; return inspection; adjustments over HTTP (four-eyes, role split, stale, impossible requests, reject).
- **Seed (`seed/inventory.seed.test.ts`)** — every seeded piece reconciles with its ledger; every status, transfer state and adjustment state the screens display exists.
- **Browser (Playwright, scratch script, 100 checks)** — list shape and 25/66 paging; **sticky toolbar stays pinned while scrolling**; search by HUID/barcode/name; every filter and URL persistence; sort/paging; the four summary tabs and drill-down; **scan box, QR payload, scanner-speed keyboard burst (opens the piece) vs slow typing (does not)**; quick view; bulk actions by status (transfer → in transit → receive; hallmarking out/back with HUID; reserve/release; mixed selection offers nothing); detail sections, valuation text, QR renders, timeline and audit tab; receive form (validation, net preview, duplicate HUID from the server, success); adjustments (request → queue → approve → ledger; store manager sees no approve controls; **self-approve disabled**; stale flagged; reject); ledger filters/empty state; **viewer: read-only UI and 403 on every stock-changing API called directly**; **390 px**: cards, filter drawer, no horizontal overflow on list/detail/ledger/transfers/adjustments/receive.
- **Gaps** — no test of `releaseExpiredReservations` on a schedule (it is not scheduled); no load test at catalogue scale; no test for partial-lot batch issue (not implemented); no automated visual regression; the E2E suite is not committed.

## 14. Pricing engine & playground (implemented — `packages/pricing-engine` 249 tests, `apps/erp` 61, `apps/api` +56, 34 browser checks)

Run: `pnpm --filter @jewellery/pricing-engine test` (no database, < 1 s), `pnpm --filter @jewellery/erp test`, `pnpm --filter @jewellery/api test`, or `pnpm test` for all.

- **Independent oracle (`oracle.test.ts`, 61)** — 60 seeded-random pieces (six purities, quoted-for-another-purity rates, every making/wastage/discount type, both `appliesTo`, intra/inter-state, cost or none) priced by `oracle/oracle.py`, a separate Python `Fraction` implementation that shares no code. Any one-paisa disagreement fails. Regenerate with `python3 oracle/oracle.py > src/oracle-cases.ts`; the seed is fixed.
- **Scenarios (`calculate.test.ts`, 61)** — nine named, hand-checked pieces: S1 22K necklace B2C (12% making, 2% wastage, stones, GST rounding a half-paisa **up**), S2 22K priced from a 24K rate inter-state, S3 18K diamond ring (per-gram, fixed-weight wastage, 5% discount, sold **below cost**), S4 925 silver anklet, S5 B2B lot of 10 (per-piece making, flat discount), S6 pendant (fixed making, 100% off making only), S7 a 3-decimal weight priced exactly, S8 24K coin, S9 950-grade. Plus behaviour of each part; **32 refusals** (negative/zero/absurd/over-precise weights, stone ≥ gross, net/fine mismatch, rate for another metal, over-100% and fractional-paise values, wastage > net, discount > base, missing state, tax > 100%) each asserting the error code; determinism (same input → identical output ×50, frozen input, clock poisoned, float noise); half-paisa rounding; **500 seeded-random lines** all satisfying the four sum identities.
- **Rule resolution (`rule-resolution.test.ts`, 49)** — the tier ladder step by step (default → category → price list → group → customer) with the shadowed-rule list; **a loud default never beats a quiet customer rule**; priority, specificity, `validFrom`, id tie-break with the ambiguity warning; independence of the three dimensions (a discount-only customer rule leaves making to the default; `NONE` wastage overrides a default wastage); effective dating (`validFrom` inclusive / `validTo` exclusive, back-to-back hand-over, inactive, channel, customer type, metal, purity case-insensitive); **order-independence over all 120 permutations of a 5-rule book and 200 shuffles × 6 situations of a 9-rule book**; refusal of duplicate ids, half-defined rules, bad dates (and *ignoring* a broken rule that cannot apply); `findAmbiguousRules` (flags equal-rank overlapping rules; ignores exclusive scopes, different metals/purities/customers/channels, non-overlapping periods, disjoint dimensions, inactive twins; agrees with what resolution warns about).
- **`priceItem` (`price-item.test.ts`, 21)** — one 22K necklace priced for a B2C walk-in, B2B retailer, a bangle (category), price list, distributor group, **customer-specific (ACME)** and a sibling in the same group — values from the oracle; overrides recorded as `OVERRIDE`; `NO_MAKING_RULE`; ambiguity warned unless overridden; **replay of a past date with the rules then in force**; 100 shuffles × 5 buyers give the identical breakdown; channel changes nothing but rules.
- **Also:** `tax-resolution.test.ts` (7: effective dating, latest wins, order-independent, refuses no-rule and same-instant ties), `fixed-point.test.ts` (22: rounding half away from zero incl. beyond `Number`, decimal tolerance/refusal, bounds), and **`independence.test.ts` (28)** — every engine source imports only siblings and `import type` from `@jewellery/types`, and touches no clock, randomness, `process`, `fetch` or `require`. Verified to fail on a deliberately bad file.
- **API (`pricing.routes.test.ts` 36, `pricing-rule.repository.test.ts` 9, `pricing-rule.validation.test.ts` 30, `pricing.seed.test.ts` 1)** — unauthenticated → 401; **all 13 roles allowed exactly where the matrix grants `pricing.manage`** (and only the two admin roles hold it); denials audited; meta lists purities with fineness; the S1 necklace to the paisa; IGST across states; a 24K-quoted 22K piece; every making × wastage mode; discounts incl. making-only; below-cost warning; **stored rules resolved per customer type with their source; a hand entry overriding only its own dimension; inactive / other-metal / future rules ignored**; 14 invalid-input cases → 400 naming the field; engine refusals surfaced as 400 with their message; unknown metal 404; **the preview writes nothing**; repeatable. Repository: an ambiguous rule is refused at create and at update (409, naming its rival), distinguishable rules accepted, a dormant twin can't be reactivated into a clash, merged-rule re-validation, customer-specific rules round-trip. The dev seed's rule book is unambiguous and resolves as the playground promises.
- **ERP logic (`parse.test.ts` 39, `playground.test.ts` 22)** — `rupeesToPaise` reads digits, not floats (`19.99` → 1999, not 1998.9999); refuses over-precision and unsafe magnitudes; **`1,5` is refused, never read as 15**; only real thousands grouping (Western and Indian) is accepted. `buildPreviewRequest` builds exact paise and leaves stored-rule charges unsent; **every example and every making × wastage × discount combination builds a request the API's schema accepts**.
- **Browser (Playwright, scratch script, 34 checks)** — the page opens on a live calculation; every figure on screen (metal value, wastage, making, discount, taxable, CGST/SGST, final, margin) equals the oracle's; buyer state flips CGST+SGST ↔ IGST with the right badge; B2B switches making and wastage to the wholesale rule and drops the retail-only offer, and the "where each charge came from" panel names the rule; ring and silver examples; below-cost warning; API refusal reason shown; recovery after fixing; over-precise weight flagged; clearing a field shows the empty state, not stale numbers; two roles without `pricing.manage` see no menu entry and cannot open the page; no console errors.
- **Gaps** — no test of the engine against real finance-signed baselines (the oracle proves the arithmetic matches the documented rules, not that the rules match the shop's; get sign-off before `PriceSnapshot`s are stored); customer-group / price-list / customer tiers are not reachable from the UI (covered by engine and API tests); no visual-regression or 390 px pass on the playground; the Playwright script is not committed; no test that a `PriceSnapshot` survives a rate change (no snapshots exist).

## 15. ERP dashboard (implemented — `apps/api` +101 tests, `apps/erp` +45, 152 browser checks)

Run: `pnpm --filter @jewellery/api test`, `pnpm --filter @jewellery/erp test`.

- **Pure logic.** `range.test.ts` (13): the IST business day (23:59:59 IST is today, 00:00 IST is tomorrow; day/leap/month arithmetic; the Monday of a week; default and explicit ranges inclusive of both ends; the previous equally-long period; daily up to 45 days, weekly beyond; zero-filled buckets). `sales-aggregation.test.ts` (17): figures worked out by hand — revenue, B2C/B2B, distinct orders (not lines), previous-period comparison, today's revenue whatever the range, margin and margin %, zero-filled daily trend, split with order counts, top-5 with name tie-break; branch and location filters; **`null` (not zero) when the previous period had no sales**; an empty shop; the 23:59 IST in / 00:01 IST out boundary; weekly buckets; frozen input; trend, split and KPIs always agree. `b2b-aggregation.test.ts` (9): pending vs decided, outstanding ignoring settled and overpaid invoices, overdue and distinct customers, *not overdue on the instant it falls due*, utilisation to one decimal, above 100% shown not capped, no limit gives null, branch scoping.
- **Live data (`dashboard.service.test.ts`, 26, real in-memory MongoDB).** A small known shop: a sold piece is *not* stock; gold and silver weights, cost, purity and location roll-ups; sellable vs reserved; stock value at cost and at the metal rate — **18K carried over from the 22K rate by fineness (₹26,610.26, checked with exact fractions)**, silver excluded and *named* for having no rate, and `null` when no rate exists; empty shop; snapshot ignores the date range. Branch/location: one branch, one location or all (two branches), same scope on operations, alerts and activity, mismatched location gives 400, unknown gives 404. Queues: counts, longest-waiting first and capped at 5, **ageing measured from the ledger entry that put the piece there** (using a shifted clock, since the ledger is append-only), over-threshold flags (3 / 14 days), a piece leaves the queue when it returns. Alerts: one per real problem with its count and link, not raised for an unlapsed reservation or a not-yet-late transfer, an adjustment turning "cannot be approved" once its piece moves, scoped to a location, empty list when nothing needs attention. Activity: newest first with who/what/where, range applied, capped at 10 with the true total. Sales/B2B without a provider give `NOT_CONNECTED`.
- **HTTP (`dashboard.routes.test.ts`, 26).** Unauthenticated gives 401 on every endpoint; **all 13 roles x 7 endpoints** allowed exactly where the matrix grants a permission, and the roles really do differ; nine invalid filter combinations x four endpoints give 400 (only-from, reversed, malformed, month 13, 30 Feb, more than a year, bad ids), exactly 366 days accepted and 367 refused; unknown/mismatched branch and location; live sections report `LIVE`, honour flags and no range for snapshots, and leak no `_id`/`__v`; provider plumbing (provenance and range passed through, **a provider is handed only the filters it honours — a location arrives as its branch when locations aren't supported**, none when none chosen); **margin withheld on the server** from a sales role without `accounting.view` — asserted on the raw response text, not just a field.
- **Isolation (`isolation.test.ts` 4, `dashboard-sample.test.ts` 6, ERP `no-invented-numbers.test.ts` 5).** No file under `src/` imports seed data or the adapter; nothing that could show a number draws one at random; the dashboard module holds no sample facts; the ERP dashboard has no `Math.random`, no rupee literal, no sample imports and no cost/revenue arithmetic. Verified to fail on a deliberately violating file. The adapter builds the identical shop every time, produces plausible order lines from the real seeded catalogue, is labelled `SAMPLE`, and its output satisfies the aggregators' invariants and responds to branch and range.
- **ERP logic (`range.test.ts` 25, `format.test.ts` 15).** Presets and month/leap/year boundaries; a bad custom range falls back and says so; **what the date inputs may commit** (half-typed year, reversed, more than a year, not a real day); change-vs-previous incl. growth from zero ("new", never a divide-by-zero); relative times; a hand-edited URL is untrusted (bad preset/branch/location dropped).
- **Browser (Playwright scratch script, 152 checks).** Everything on screen equals the API's own response (each KPI, trend total and column count, top lists, gold/silver/available/reserved/value, by-location/metal/purity, queue counts, alert rows and links, B2B overdue and credit, activity rows); tooltip on hover and on keyboard focus, and the exact figures in each column's accessible name; refresh re-fetches; every preset and a custom range; **two fast date edits both stick, an inverted or half-typed range keeps the inputs and explains itself without touching the URL**; reload keeps the range; garbage URL tolerated; location, branch and their interplay reach every section that honours them; Reset; empty states (no sales/activity in range, a location with no stock); a delayed section shows skeletons and `aria-busy` while the rest are filled; a failed section shows Retry while the rest render, and Retry recovers; eight roles see exactly their sections with margin `Restricted` and the API refusing the rest with 403; 390 / 820 / 1920 px: no horizontal scroll, tiles fully visible, one column on a phone and one row of six on a wide screen.
- **Gaps** — Sales and B2B are exercised only against sample facts (there is no real source to test against); no test of the dashboard with thousands of ledger entries or a large catalogue (aggregations use status/location filters but are not load-tested); the browser suite is a scratch script; no visual-regression tests; the intermittent full-suite failure described in progress.md (Phase 2.5) is unresolved.

## 16. B2C storefront (implemented — `apps/api` +66 tests, `apps/b2c-store` 39, 156 browser checks)

Run: `pnpm --filter @jewellery/api test`, `pnpm --filter @jewellery/b2c-store test`.

- **API (`storefront.routes.test.ts`, 66, real in-memory MongoDB).** Only published designs are visible; a price equals the engine's for the same inputs and carries its breakdown and basis; **every `ON_REQUEST` reason** (no weight, stones without a stone value, no rate, no rule, no GST rule); price changes when the rate changes (no caching); filters (category incl. descendants, metal, purity, price range on live prices, in stock) and sorts; facets from the unrefined scope; search over name / SKU / tags / category / collection; availability including sized designs (sum over sizes); bestsellers from real sales only; cart quote re-prices server-side and caps quantity; newsletter validation and idempotence; rate limiting; public routes need no login and never leak cost or margin.
- **Storefront logic (39).** URL <-> listing-param round trips and garbage tolerance, rupees vs paise, the bag store (quantity limits, merging, persistence of identifiers only), JSON-LD (offer only when a price is available, no rating claims).
- **Isolation.** `isolation.test.ts` still holds: nothing under `src/` imports seed data; no randomness in production or seed code.
- **Browser (Playwright scratch script, 156 checks).** Everything on screen equals the API's response (prices to the paisa, breakdown total, bag totals); the price-on-request piece; out-of-stock size; gallery swipe / zoom / lightbox; sticky buy bar; filters, sort, pagination, URL persistence; phone filter sheet, menu and search; bag limits; checkout validation and the "ordering isn't open" state; wishlist persistence; account / orders notices; SEO tags, JSON-LD, sitemap, robots, 404 status; overflow at 390 / 820 / 1440 / 1920 px; no console errors.
- **Gaps** — no test with real payment or order flows (none exist); the storefront seed has no test of its own; no visual-regression or Lighthouse budget; no load test of the list endpoint (stock and prices are computed per request over the whole published catalogue — fine for hundreds of designs, needs a cached price index at thousands); the browser suite is a scratch script.

## 17. Checkout, orders & payments (implemented — `apps/api` +83 tests, `apps/b2c-store` +7, 56 browser checks)

Run: `pnpm --filter @jewellery/api test`, `pnpm --filter @jewellery/b2c-store test`. The browser script needs a fresh `dev:memory` each run (it consumes stock).

- **State machines (`order-status.test.ts`, 13).** Every status has a rule; checkout path legal; nothing reaches PAID/CONFIRMED except through payment; failed→retry and late-success; no going back after shipping; REFUNDED final; customer-cancellable set; payments only move forward (stale "authorized" after "captured" ignored, FAILED→CAPTURED allowed); derived REFUNDED/PARTIALLY_REFUNDED; access tokens.
- **HTTP (`checkout.routes.test.ts`, 70, real in-memory MongoDB, sandbox gateway).**
  - *Verification and trust:* server-side totals, delivery fees, GST split by state with an unchanged total, per-line issues, refusal with no gateway; six kinds of client-supplied money field refused; a low `agreedTotal` buys nothing.
  - **Price change during checkout:** refused with the fresh verification and nothing created; accepted at the new price; a later rate move leaves order, snapshot and payment amount unchanged; snapshot/order updates and deletes rejected.
  - **Stock disappearing:** last piece sold between verify and order; two shoppers racing for one piece (exactly one wins); piece gone after ordering but before payment → refunded.
  - **Successful payment:** initiation through the abstraction; capture by webhook and by the customer's return; browser claims ignored; authorised-then-captured; stale events; gateway down; no-provider 503; the ledger's `SALE` per piece tied to the order.
  - **Payment failure:** order PAYMENT_FAILED, pieces still held, retry succeeds; failure noticed on return; closing the page (unless already paid); late success honoured; wrong amount refunded.
  - **Duplicate webhook:** twice, three at once, new event id for the same fact, racing the customer's return, a second payment for a paid order refunded; bad/tampered signature 400; unknown payment 409 (resend); unknown provider.
  - **Cancellation:** unpaid (pieces freed, payment voided), late capture after cancel refunded, paid (pieces `RETURNED`, refunded, REFUNDED), asynchronous refund and its duplicate confirmation, gateway refusing a refund then retry, after PACKED refused, twice refused, wrong token 404, provider asked before cancelling.
  - **Expiry and refunds:** lapsed holds cancelled; running holds untouched; paid-at-the-last-second kept; idempotent; partial → full refund, over-refund refused, racing refunds return once.
  - Idempotency (same key replayed, reused key refused, simultaneous), access tokens, no internal data in the customer's view.
- **Storefront logic (`checkout.test.ts`, 7).** Contact validation with the API's schema; what the browser sends contains no price; request signatures; order wording.
- **Browser (Playwright, 56 checks).** Whole flow through the sandbox payment page: address validation, delivery options and fees, review total equals the API's, GST label, button amount, double-click places once, held stock, confirmation, reload, another browser denied; decline then retry; back out then cancel (stock returns); cancel-and-refund of a paid order; price changed on review (accept to continue); stock taken by another shopper; phone layout without overflow; no console errors.
- **Gaps** — no real gateway (the sandbox proves the contract, not a vendor's quirks); no test of a crash between an order's cancellation and the return of its pieces; the expiry sweep is not tested as a running timer; no load test of concurrent checkouts; the browser suite is a scratch script; staff fulfilment/return transitions have only the graph tested.

## 18. B2B wholesale (implemented — `apps/api` +101 tests, `apps/b2b-portal` 9, 101 portal + 29 ERP browser checks)

Run: `pnpm --filter @jewellery/api test`, `pnpm --filter @jewellery/b2b-portal test`. The browser scripts need a fresh `dev:memory` each run (they consume stock and change state).

- **Logic (`b2b-logic.test.ts`, 22).** PO / sales-order / payment lifecycles (nothing invoiced before allocated, terminals stay terminal); credit position (outstanding, committed, overdue, negative available); the credit check at the boundary (exactly the limit passes, a rupee over stops), committed orders, credit hold, overdue-only-when-set, several reasons at once, actionable messages; invoice status; ageing buckets; the strict request schemas.
- **HTTP (`b2b.routes.test.ts`, 79, real in-memory MongoDB).**
  - *Access:* 401 without a login; staff out of the portal and buyers out of the seller API; an unlinked or inactive buyer refused; **another customer's documents are simply 404**; seller actions by permission, not role.
  - *Pricing:* default, then price list, then group, then customer-specific (basis and exact amounts); no leak of one customer's price to another; follows the metal rate; price on request by policy and for unpriceable pieces.
  - *Catalogue and quick order:* offered-only, exact stock, MOQ, sized designs by size SKU, filters / sort / paging / facets; SKU resolution (any case, merged rows), blocking vs warning problems, strict bodies (a price is refused), GST per address, credit preview with an actionable reason.
  - *Purchase orders, quotations, negotiation:* draft → edit → re-priced submit; MOQ and offered range enforced on the backend; over-stock and price-on-request lines accepted; approval into a frozen sales order; quotation with percentage and target-price concessions (overrides recorded), acceptance at the quoted price after a rate move, expiry, counter → revise → only the latest version acceptable, decline, reject.
  - *Credit:* held order (no allocation, no invoice), approval only by the override permission with a reason and an audit entry, override refused (403) without it, approved-uninvoiced orders count, **two approvals racing for one headroom → exactly one**, credit hold, overdue rule, release without an override once the customer has paid.
  - *Fulfilment:* allocation in the ledger, all-or-nothing with a line-by-line shortfall, **racing allocations of the last pieces → exactly one**, invoicing (ledger SALE, due date, CGST+SGST / IGST), no invoice before allocation, cancel releases stock, invoices frozen.
  - *Payments:* all seven methods; nonsense refused; **entering or verifying a payment never marks an invoice paid**; nobody verifies their own entry; unverified payments can't be applied; partial, split and multi-payment allocation and settlement; never over-applied (invoice, payment, another customer's invoice); **racing allocations don't over-pay**; reversal restores the debt; rejection; customer-reported payments; audit of every step.
- **Portal logic (9).** Cart store (SKU + quantity only, no price ever stored), quick-order paste parsing, money, order steps.
- **Browser (Playwright, 101 portal + 29 ERP).** Portal: sign-in and guards, dashboard equals the API, catalogue prices / stock / filters / MOQ / price-on-request, quick order (validation, paste, totals equal the backend's), cart → PO, seller approval → order → invoice, payment report → verify → apply (invoice stays unpaid until applied), ageing, quotation negotiation, **credit warning in the cart, backend hold, explanation on the order, release**, account, tenant isolation, phone / tablet layout, sign-out. ERP: review queue, approve, quote, quotations, credit approval with reasons, shortfall on allocation, allocate and invoice, edit terms + audit, record / verify (maker–checker error shown) / apply payments, viewer sees no actions.
- **Gaps** — no test of a *crash* mid-transaction (only concurrency); the catalogue's in-memory filtering isn't load-tested; e-invoicing, returns and credit notes are untested because unbuilt; the browser suites are scratch scripts; the long-running intermittent full-suite failure was a test-harness port collision (supertest's ephemeral wildcard-bound servers vs another process on 127.0.0.1) and is fixed in `test/setup.ts` — `loginAs` now also logs any failed sign-in.

## 19. Purchasing (implemented — `apps/api` +101 tests; no browser checks yet)

Run: `pnpm --filter @jewellery/api test`.

- **Logic (`procurement-logic.test.ts`, 75).** Every (action × status) pair for the requisition, purchase-order, supplier-invoice and supplier-payment state machines, exhaustively (allowed exactly from the listed statuses, refused with a typed 409 from every other); the transition graphs match what the rule tables imply, and terminal statuses have no exits; a derived purchase-order move (`receive`) is still checked against the graph; a purchase order's status derived correctly from received-so-far numbers (nothing / some / every line closed); line arithmetic (`lineIsClosed`/`lineOutstanding` governed by weight for a weight-tracked type, by quantity otherwise; `totalsOf`; paisa rounding); the line and receipt-line validation schemas (a weight-tracked line needs metal/purity/weight/rate, finished jewellery needs metal/purity/rate but not weight, a consumable needs neither; strict bodies; a receipt line defaults quantity to 1 and needs a location).
- **HTTP (`purchasing.routes.test.ts`, 26, real in-memory MongoDB).**
  - *Access:* 401 without a login; view open to `purchasing.view`; create/approve/receive/cancel each need their own permission (a warehouse manager can receive but not approve; an accountant can't receive); supplier invoices/payments need `accounting.create_payment` to write.
  - *Suppliers:* list with live payables position, create/update.
  - *Requisitions:* draft → submit → approve (audited, with the total); reject with a reason, can't approve twice; a purchase order can only be raised against an `APPROVED` requisition, and converting it links back.
  - *Purchase orders:* draft → submit → approve, value computed from fine weight × rate server-side (a client-sent `value` refused, strict body), approval audited with the total and supplier.
  - **Goods receipt — the five explicitly required scenarios:** *partial receipt* (less than ordered leaves the PO `PARTIALLY_RECEIVED`, the rest received later closes it, two receipts posted, two ledger `PURCHASE_RECEIPT` entries); *over-receipt* (more than outstanding refused with `OVER_RECEIPT` and nothing written, by weight for a metal line and by piece count for a quantity line, including over-receipt on a second receipt after part of the line is already used); *incorrect purity* (a different purity than ordered refused with `PURITY_MISMATCH`, nothing written); *weight discrepancy* (a scale reading >2% off a stated `expectedGrossWeight` refused without a note, flagged with the exact variance once a note is given, the item created with the actual weighed amount; within tolerance is silent; a partial receipt with no stated expectation is never flagged); *cancellation* (a PO cancelled before receipt; a partially-received PO cancelled — what already arrived stands, still `AVAILABLE`; a fully received PO can't be cancelled). Also: receiving against a non-approved PO or an unknown line index refused; finished jewellery creates one `UNIT` item per piece, a consumable creates none.
  - *Supplier invoices:* recorded against a PO, linking back; cancelled while unpaid, refused once a payment is allocated.
  - *Supplier payments:* recorded and applied to settle an invoice exactly; never over-applied (beyond the invoice, beyond the payment); reversed — allocations undo and the invoice is owed again, audited.
  - *Outstanding & dashboard:* ageing against the due date, the dashboard's counts.
- **Gaps** — no browser/Playwright checks yet (the ERP screens — dashboard, suppliers, purchase orders with the folded-in requisitions panel and receive-goods form, goods receipts, outstanding — are typechecked and linted but not run against a live browser); no test of a crash mid-transaction; a supplier return isn't built, so isn't tested; GST input-credit tracking on supplier invoices is a single entered `taxAmount`, not independently verified against a CGST/SGST/IGST breakdown.

## 20. Manufacturing & job work (implemented — `apps/api` +199 tests; no browser checks yet)

Run: `pnpm --filter @jewellery/api test`.

- **Logic (`manufacturing-logic.test.ts`, 92).** Every (action × status) pair for the production-order and job-work-order state machines, exhaustively; the transition graphs match what the rule tables imply, terminal statuses have no exits; a derived job-work move (`returnGoods`) is still checked against the graph; `reconcile()` (issued − returned − finished − wastage): balances to zero, a tiny rounding difference within a fixed tolerance is not a discrepancy, a real gap beyond it is flagged and carries the note, more returned than issued is a *negative* discrepancy (never clamped away); the `bom`/`returnJobWork` validation schemas (a positive expected weight required, strict bodies, a return needs something to record unless it's the final close).
- **HTTP (`manufacturing.routes.test.ts`, 15, real in-memory MongoDB).**
  - *Access:* 401 without a login; view open to `production.view`; create and QC-approve each need their own permission.
  - **Production order — the full flow:** issuing material moves a real `InventoryItem` through the ledger (`AVAILABLE → IN_MANUFACTURING`, a second ledger entry beyond its original receipt, never just a quantity update) and refuses issuing something not `AVAILABLE`; start → submit-for-QC (records actual weight/wastage/labour cost) → pass → complete creates a real, traceable finished `InventoryItem` (`manufacturingInfo.productionOrderId` set, one `MANUFACTURING_RECEIPT` ledger entry, audited); QC failure → rework → a second QC pass completes normally; cancellation returns whatever was issued and not yet consumed, ledgered, audited.
  - **Material reconciliation — the explicitly required scenarios:** a discrepancy beyond tolerance refused without a note and flagged with one (exact `discrepancyGrossWeight`); unused issued material returned *whole*, credited in the reconciliation and back to its stock at the location it came from (not the manufacturing unit); the reconciliation screen lists every issued order, flags discrepancies clearly, sorts them first, and a discrepancy-only filter excludes clean orders.
  - **Job work:** issuing moves material to the job worker through the ledger; a partial return (some finished, more expected) is *not* flagged as a discrepancy, only the closing (`final: true`) return is, and only then must a real gap carry a note; won't return an item that wasn't issued (400) or the same item twice (409); can only issue a `DRAFT` order and cancel only before issue.
  - *Dashboard:* counts orders by stage.
- **Two real bugs the tests caught before this note was written:** returning unused material (or cancelling) tried to send it back into the manufacturing-unit/job-worker location itself, which `MANUFACTURING_RECEIPT`/`JOBWORK_RECEIPT`'s movement rule refuses (that destination must be a real stock location) — fixed by remembering, per issued item, the location it came from (`MaterialItemRef.fromLocationId`) and returning it there. A partial job-work return was flagging `hasDiscrepancy: true` from the naturally-incomplete running total — fixed by only evaluating the discrepancy flag on the closing (`final`) return.
- **Gaps** — no browser/Playwright checks yet (the ERP screens — production orders, job work, quality control queue, reconciliation — are typechecked and linted but not run against a live browser); stone-material reconciliation isn't separately tested (the same weight-based math covers it, but no test issues a `LOOSE_STONE` batch specifically); no test of a crash mid-transaction.

## 21. Hallmarking (implemented — `apps/api` +40 tests; no browser checks yet)

Run: `pnpm --filter @jewellery/api test`.

- **Logic (`hallmarking-logic.test.ts`, 26).** Every (action × status) pair for the batch state machine, exhaustively (allowed exactly from the listed statuses, refused with a typed 409 from every other, including that `receive` is allowed from either `IN_TRANSIT` or `AT_CENTRE`); the transition graph matches what the rule table implies, `RECEIVED`/`CANCELLED` have no exits; the workflow's own vocabulary matches the spec exactly (`PENDING | IN_TRANSIT | AT_CENTRE | RECEIVED | CANCELLED`, with `VERIFIED`/`FAILED` deliberately *not* batch statuses); `checkLineOutcome` refuses a decision before `RECEIVED` and refuses a second decision once one is made; the batch-creation and receive-line validation schemas, including that a given HUID must be exactly six letters/digits (reusing the one shared `zHuid` shape, not re-implementing it).
- **HTTP (`hallmarking.routes.test.ts`, 14, real in-memory MongoDB).**
  - *Access:* 401 without a login; view open to `inventory.view`; writes need `inventory.create`.
  - *Assaying centres:* must reference a real `HALLMARKING_CENTER`-type location, refused against any other type; list and update (activate/deactivate).
  - **The full flow, with real ledger assertions:** create (nothing moves yet, item stays `AVAILABLE`) → dispatch (`HALLMARKING_OUT` ledger entry, item moves to `HALLMARKING` status at the centre's own location, audited with centre and item count) → arrive (logistics-only, no further ledger entry) → receive (`HALLMARKING_IN` ledger entry, item back to `AVAILABLE` at the location it was sent *from*, not the centre; HUID/certificate/date recorded, `hallmarkStatus` → `HALLMARKED`) → verify (per piece, audited with the HUID) or fail (with a required reason, audited); a piece returned without a HUID stays `hallmarkStatus: PENDING`, never silently upgraded.
  - **The HUID is unique where applicable — explicitly tested:** receiving a second, different item with a HUID already assigned to another is refused (`DUPLICATE_IDENTIFIER`, 409, nothing written to the second item); a HUID is stored uppercase, so a lowercase and uppercase entry collide as the same mark.
  - **A batch's receipt is all-or-nothing:** a receive call naming only some of a batch's pieces is refused (409, nothing moves, not even the pieces that *were* named) — this is a real bug the test caught and the fix (§16.3) closed, before it could leave a piece stranded in `HALLMARKING` while its batch read `RECEIVED`.
  - *Per-piece decisions:* can't verify/fail before the batch is `RECEIVED`; can't decide a piece twice.
  - *Cancellation:* only before dispatch; refused once `IN_TRANSIT`.
  - *Sending:* refused for an item that isn't `AVAILABLE` (e.g. already away at hallmarking).
  - *Item history:* every batch a given piece has ever been part of, newest first, empty for an untouched piece — the read the Inventory Item Detail screen's "Hallmarking History" is built on.
  - *Dashboard:* counts by each piece's *effective* status (a batch's shared stage until a piece has its own outcome).
- **A real bug the tests caught before this note was written:** `receiveHallmarkingBatch` accepted a receive call that named only some of a batch's pieces, moved just those through the ledger, but still flipped the *whole batch* to `RECEIVED` — leaving the omitted pieces stuck in `HALLMARKING` status/location with no way back except a manual fix. Fixed by refusing any receive call that doesn't account for every piece on the batch (§16.3).
- **Gaps** — no browser/Playwright checks yet (the ERP screens — hallmarking dashboard with its Pending/In Transit/Received/Verified/Failed tabs, assaying centres, and the item-detail hallmarking-history section — are typechecked and linted but not run against a live browser); no test of a crash mid-transaction; no test of a centre being deactivated mid-batch (the batch itself is unaffected either way, since the centre reference is a snapshot).

## 22. Returns, Exchange & Repair (implemented — `apps/api` +191 logic tests, +33 HTTP tests; ERP/storefront/portal typechecked, linted and production-built, no browser checks yet)

Run: `pnpm --filter @jewellery/api test`.

- **Logic** — one file per module, each following the same shape as hallmarking's: every `(action × status)` pair for the state machine, exhaustively; the transition graph matches what the rule table implies; terminal statuses have no exits; the workflow's own vocabulary matches the spec; and the request/decision validation schemas.
  - `returns-logic.test.ts` (48) — `REQUESTED → APPROVED/REJECTED → RECEIVED → INSPECTED → SETTLED`, `CANCELLED` reachable only from `REQUESTED`/`APPROVED` (never once stock is in flux); `requestReturnSchema`/`receiveReturnSchema`/`inspectReturnSchema`/`settleReturnSchema` shape and strictness.
  - `exchange-logic.test.ts` (16) — `DRAFT → ASSESSED → COMPLETED`/`CANCELLED`, with `assess` deliberately re-enterable from `ASSESSED` (re-weighing isn't a correction); `oldJewelleryAssessmentSchema` refuses stone weight exceeding gross weight and refuses `valuation` as an input field (computed server-side only).
  - `repair-logic.test.ts` (127 — the largest, matching its eleven statuses and twelve actions) — the full `INTAKE → INSPECTED → ESTIMATED → APPROVED → IN_PROGRESS → QC_PENDING → READY → DELIVERED` path, `DECLINED` and `CANCELLED` as early exits, `QC_FAILED → IN_PROGRESS` rework loop, `cancel` reachable from every working status but not `READY` or a terminal one; `repairIntakeSchema`'s branch (an existing item needs only its id; a customer-owned piece needs metal/purity/weight); `recordRepairWorkSchema`'s stone-weight-cannot-exceed-gross-weight check; `estimateRepairSchema` refuses a hand-entered `total`.
- **HTTP, with real ledger assertions (`returns.routes.test.ts` 11, `exchange.routes.test.ts` 9, `repair.routes.test.ts` 9, real in-memory MongoDB):**
  - **Returns — B2C and B2B, identical discipline:** a return resolves the exact `InventoryItem` from the order's/sales order's own `allocations` (never a client-supplied item↔order pairing); the full flow (`ledger.map(movementType)` proven to read `[PURCHASE_RECEIPT, SALE, RETURN, RETURN]` — receive then inspect, both `RETURN`, distinguished by `fromStatus`/`toStatus`) through to `SETTLED`, with the audit log checked at each step; inspection landing a piece `DAMAGED` instead of `AVAILABLE`; reject leaves the item `SOLD` and posts no further ledger entry at all; cancel allowed before `RECEIVED`, refused (409) after; a piece not actually sold on the named order is refused (400) before anything is touched; **a HUID presented at receipt that doesn't match what was recorded at request time is refused (409), the item never moves**; a weight that has drifted needs a discrepancy note to post (400 without one, 200 with); a second open return against an already-covered piece is refused (409).
  - **Exchange:** valuation is computed from the assessed purity's fineness and the given rate via the shared `valueOfMetal` — cross-checked against a hand-computed `netWeight × ratePerGram` (business-rules.md §18.2: the rate is quoted *for the piece's own purity*, so fineWeight is informational, not part of the sum); a deduction reduces the valuation but never below zero; an unrecognised purity for the metal is refused; completing posts exactly one `EXCHANGE_IN` ledger entry creating the old piece (`RAW_MATERIAL`, `UNIT`, `cost` = the valuation); a positive difference (customer owes) and a negative one (business owes, never clamped away) are both asserted; re-assessment before completion and cancellation before the piece is taken in both work, completion is refused once cancelled.
  - **Repair:** QC decisions need `repair.approve`, not just `repair.create` (a real separation-of-duties check, not just a route smoke test); intake against a piece that isn't currently `SOLD` is refused (409); the full flow for an already-sold piece — intake posts `REPAIR_OUT` (ledger: `[PURCHASE_RECEIPT, SALE, REPAIR_OUT]`), through estimate, approval, start, `recordWork` (defaults `finalCharges` from the approved estimate when none is given), a QC fail → rework → a second QC pass → deliver, ending the ledger with `REPAIR_RETURN` (`UNDER_REPAIR → SOLD` — **never onto sellable `AVAILABLE` stock**); intake for a piece never held before creates a fresh `isCustomerOwned` item via `REPAIR_INTAKE` (one ledger entry, `cost: 0`) and, on delivery, ends at `RETURNED_TO_CUSTOMER` (never `SOLD`); declining the estimate hands the piece straight back (`REPAIR_RETURN`) without ever starting work; cancellation from `IN_PROGRESS` still returns the piece, but is refused once `READY` or `DELIVERED`.
- **Customer-facing access (`aftersales-customer-access.routes.test.ts`, 4):** a guest cannot even reach the token check with a malformed body (itemIds/lineRefs must be non-empty *before* the order lookup runs — an intentional ordering, since validation is cheaper than a DB round-trip); the real order-access-token is required to request or list a B2C return (a wrong/missing token is a 404, indistinguishable from no such order — business-rules.md §11.8's rule, unchanged for this endpoint); a request naming an `orderId` or a raw `InventoryItem` id in the body is refused outright (400 — the customer-facing schema has no such fields, only `lineRefs`); a B2B buyer can request/list against their own sales order but gets a 404 naming another customer's (`reads.salesOrder(id, {customerId})` is the ownership check — there is nowhere in the request to even name a different customer); staff cannot use the buyer-only portal door.
- **A structural fix made along the way, not a bug in the new code:** `receiveNewInventoryItem`'s `movementType`/`referenceType` unions and `useApiMutation`-style ERP action hooks' `onSuccess` callback type both needed widening to accommodate the new creation movements and a result-carrying callback — caught by `tsc`, not by a test, but recorded because the fix touched shared inventory/ERP code, not just the new modules.
- **Gaps** — no browser/Playwright checks yet (the ERP's Returns/Exchanges/Repairs screens, and the storefront/portal Returns sections, are typechecked, linted and production-built, not run against a live browser); no order/item picker in the ERP's "new return"/"repair intake" forms (the order id is entered directly — see progress.md's Phase 8 notes on why); no test of a return/repair racing another stock movement on the same item (the ledger's own compare-and-set concurrency guarantee, proven exhaustively elsewhere — §3, §13 — is relied on here rather than re-proven per module); no return deadline/eligibility window (untested because unbuilt — a business decision still to make).

## 23. Accounting (implemented — `apps/api` +15 HTTP tests added directly to `b2b.routes.test.ts`/`purchasing.routes.test.ts`; ERP typechecked, linted and production-built, no browser checks yet)

Run: `pnpm --filter @jewellery/api test`. Unlike every other workflow module, there is no dedicated logic-only test file: the posting engine has no pure state machine to test in isolation (no statuses, no action table) — its whole job is resolving accounts and checking a balance, which only means something against a real chart of accounts. The tests live where the postings actually happen: appended to the existing B2B and Purchasing HTTP suites, asserting on the journal entries those flows now also produce — deliberately, so a change to the real invoicing/payment flow and a change to its accounting entry are proven together, not in two suites that could drift apart.

- **B2B — sales side (`b2b.routes.test.ts`, +9, appended as "accounting: every completed financial transaction posts a balanced journal entry"):**
  - Invoicing posts one journal: Dr Accounts Receivable (the invoice total), Cr Sales (gross taxable) and GST Payable, and — because every `InventoryItem` already carries its own book cost — Dr Cost of Goods Sold / Cr Inventory at the *actual* cost of the pieces sold (2 pieces at 100.00 = 200.00), with `totalDebit === totalCredit` asserted directly off the written document.
  - A concession (`discountPercent` on a quotation line) shows up as its **own** Discount Given debit line, and Sales is credited at the pre-discount gross — never netted invisibly into a smaller revenue figure.
  - Payment allocation posts Dr Bank (NEFT) / Cr AR for exactly the amount *applied in that call*, not the payment's full face value; a separate test proves CASH lands in the Cash account, not Bank. Reversing the payment posts a second entry with every line's direction flipped, referencing the same payment.
  - The Receivables Dashboard, Outstanding list and Ageing report (new `/api/accounting/receivables/*` endpoints) agree with the invoice before and after it's paid — the invoice drops out of Outstanding the moment it's fully allocated.
  - Chart of Accounts: deactivating the *only* active account holding a system role (Accounts Receivable) is refused (409) — the posting engine must always have somewhere to post.
  - A credit note debits Sales and GST Payable and credits AR for the total; cancelling it posts the exact reversal, and the net effect of issue + cancel across every account touched is proven to be zero.
  - Permission split: `accounting.view` reads; only `accounting.manage` can create an account or a note — a B2B_MANAGER token (view only) is refused.
  - The trial balance always balances after a real invoice is posted.
- **Procurement — purchase side (`purchasing.routes.test.ts`, +6, same describe-block pattern):**
  - A weight-tracked (ledger-tracked) supplier-invoice line posts Dr Inventory + Dr GST Receivable, Cr Accounts Payable — no Purchases line at all.
  - A `CONSUMABLE` line (which creates no `InventoryItem`) posts to Purchases instead, proving the split follows procurement's own existing `isLedgerTracked` rule rather than a second decision.
  - Cancelling an unpaid supplier invoice reverses exactly what it posted — the net movement on every account it touched, summed across the issue and its reversal, is zero.
  - Supplier payment allocation posts Dr AP / Cr Bank for what was applied; reversal posts the opposite.
  - A debit note debits AP and credits Inventory and GST Receivable.
  - The payables side (Accounts Payable's credit balance) shows up in the same trial balance as the receivables side — one ledger, both directions.
- **Gaps** — no browser/Playwright checks yet (Receivables, General Ledger, Chart of Accounts, Credit Notes and Debit Notes screens are typechecked, linted and production-built, not run against a live browser); no test of a B2C sale posting (deliberately not wired this phase — see progress.md); no manual/free-form journal entry to test, because none exists by design; no multi-currency, cost-centre or budget behaviour, because none of that is built (not a full accounting package, by the task's own instruction).
